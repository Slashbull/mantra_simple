# Minimal requirements for fastest deployment
# Remove psutil temporarily to speed up

streamlit==1.32.0
pandas==2.0.0     # Older version, faster install
plotly==5.17.0    # Slightly older, more stable
numpy==1.24.0     # Compatible older version
requests==2.31.0  # Still needed for data loading

# Note: psutil removed for faster deployment
# openpyxl removed as not essential
