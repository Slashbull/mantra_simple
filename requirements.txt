# Core dependencies for M.A.N.T.R.A. Streamlit deployment
# Complete list with all required packages

# Core packages
streamlit==1.32.0
pandas==2.2.0
plotly==5.19.0
numpy==1.26.3

# Additional required packages
psutil==5.9.8      # System monitoring for health panel
requests==2.31.0   # HTTP requests for data loading
openpyxl==3.1.2   # Excel file support (if needed)

# All dependencies for production deployment
