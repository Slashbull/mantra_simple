# Core dependencies for Streamlit Cloud deployment
# Minimal, stable versions for permanent production use

streamlit==1.32.0
pandas==2.2.0
plotly==5.19.0
numpy==1.26.3

# No additional dependencies needed
# Keeping it minimal for fastest cloud deployment
# All features work with just these 4 packages
