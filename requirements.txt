# Optimized for Streamlit Community Cloud
# These versions are known to work well on the platform

streamlit==1.32.0
pandas==2.0.3
numpy==1.24.3
plotly==5.14.1
requests==2.31.0

# Note: psutil excluded for faster deployment
# openpyxl excluded as not essential
